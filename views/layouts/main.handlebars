<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech-Critique Bloggersphere</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.5.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <header id="navheader" class="text-white">
        <section>
            <h1>Tech-Critique Bloggersphere</h1>
        </section>
        <nav class="d-flex justify-content-evenly">
        {{#if loggedIn}}
            <a href="/" class="btn btn-secondary">Home</a>
            <a href="/api/users/dashboard" class="btn btn-secondary">Dashboard</a>
            <a href="/api/users/logout" class="btn btn-danger">Logout</a>
        {{else}}
            <a href="/" class="btn btn-secondary">Home</a>
            <a href="/login" class="btn btn-secondary">Login/Sign Up</a>
        {{/if}}  
        </nav>
    </header>
    <main>
        {{{body}}}
    </main>
</body>
</html>